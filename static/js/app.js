/**
 * Application JavaScript code for Artint view
 * 
 */
var Artint = function() {

    /**
     * Registers all event listeners we need
     */
    var registerEvents = function() {

    };

    /**
     * Public API of the Artint view module
     */
    return {
        init: function() {
            registerEvents();
        },
    };
}();

/**
 * Initialize the Artint view when the document is ready
 */
jQuery(document).ready(function() {
    Artint.init();
});
